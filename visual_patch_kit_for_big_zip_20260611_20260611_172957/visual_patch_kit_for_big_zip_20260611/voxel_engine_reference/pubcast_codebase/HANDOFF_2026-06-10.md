# PubCast Integration Handoff — 2026-06-10

## Goal
Inventory, organize, and wire together all Pubcast system components found across Downloads and `big zip` — identify the missing `thinking_context/` package, select the best entry-point main.py, integrate friday-Lunch's unique EQ, wired orchestration, and pipeline modules.

## What Was Done This Session

### 1. Inventory & Analysis
- Analyzed all main.py variants across Downloads, friday-Lunch, and pubcast_codebase. **Existing `pubcast_codebase/main.py` (2750 lines, 12-step boot) is the correct entry point** — not replaced.
- Identified `thinking_context/` as the **only truly missing module** referenced by main.py:114 but never placed in codebase. Found 5 extracted copies across Downloads.
- Found **Variant B** (`Pubcast-AI-3-20-26` directory) is best — has relative import fix in `jeremy.py` and guarded FastAPI import in `__init__.py`.
- Confirmed `modules/jeremy_cricket.CricketKeeper` (22 KB, SQLite memory store) and `thinking_context/jeremy.JeremyCricket` (35 KB, full conductor) are **NOT the same** — both coexist.
- Mapped the nudge flow: thinking_context watches rooms → detects topic/profile relevance → nudges specific AI with contextual prompt → AI responds unprompted into room.

### 2. thinking_context/ Placed
- Copied Variant B (85,945 bytes, 8 files) → `pubcast_codebase/thinking_context/`
- All 8 files verified by SHA256
- Already has its own `nudge_queue.py` (clean NudgeQueue/NudgeConsumer, 166 lines) — no duplication needed from wired_system.py

### 3. friday-Lunch Extraction
- Created `pubcast_codebase/modules/wired/` subpackage
- Extracted **23 files** from `friday-Lunch (1).zip` into `modules/wired/`:
  eq_adaptor, eq_card_selector, eq_disambiguation_engine, eq_integration_layer,
  orchestrator_wired, wired_orchestrator, wired_system, wired_server,
  conversation_state, response_composer, gpt_adapter, hardened_websocket,
  security, rate_limiter, meaning_maker, unified_runtime_boot, voxel_renderer,
  pubcast_voxel_hollow_patch, NUDGE_MINIMAL, animation_presets_complete,
  test_hardening, orchestrator_hardening, ethereal_avatar_generator
- `wired_system.py` NOT integrated as whole (class conflicts: JeremyCricket, BotManager, InputScorer, CharacterEngine)
- NudgeQueue/NudgeConsumer already exist in `thinking_context/nudge_queue.py` — clean version

### 4. Guarded Imports Added to main.py
Added after existing `ConversationOrchestrator` guard block (lines 294-316):

```python
_HAS_WIRED = False
_HAS_EQ    = False
# guarded: WiredOrchestrator (wired_orchestrator.py)
# guarded: EQOrchestrator, EQOrchestratorBridge (eq_integration_layer.py)
# guarded: PubCastAdapter (pubcast_adapter.py)
```

All `except ImportError:` handlers set symbols to `None` — server boots regardless.

### 5. PubCastAdapter Created
`modules/wired/pubcast_adapter.py` — wraps Hub (`get_recent_history`) + BotManager (`nudge`) into `ContextHost` protocol for thinking_context.

## Key Decisions
- **Keep `pubcast_codebase/main.py`** as primary entry point
- **New modules go into `modules/wired/`** subpackage — avoids namespace collisions with existing `modules/`
- **Variant B** of thinking_context (from Pubcast-AI-3-20-26) — most complete
- **`wired_system.py` NOT copied as whole** — only NudgeQueue/NudgeConsumer used (already in thinking_context/)
- **Parallax Level Builder excluded** — development tool only

## Architecture Overlap Warning
Three incompatible architectures exist:
1. **(A) event-driven v5.5** in `pubcast_codebase/modules/` — existing spine
2. **(B) flat-file EQ+wired** in `friday-Lunch/` — now in `modules/wired/`
3. **(C) sequential pipeline** in `pubcast_runtime.py`/`director.py`/`response_composer.py` in big zip root

**Plan**: Use A as spine, B and C layered as optional systems.

## Next Steps
1. **Wire PubCastAdapter into main.py boot sequence** — after BotManager step, construct adapter, pass to thinking_context.mount()
2. **Run `python main.py`** — verify all 12 boot steps complete, `_HAS_THINKING_CONTEXT = True`, `_HAS_WIRED`/`_HAS_EQ` resolve
3. **Run existing test suite**
4. **Optional:** Add `_HAS_ADAPTER` flag usage in boot sequence

## Files Changed/Created This Session
- `pubcast_codebase/thinking_context/` — 8 files (Variant B, placed)
- `pubcast_codebase/modules/wired/` — 23 friday-Lunch files
- `pubcast_codebase/modules/wired/pubcast_adapter.py` — PubCastAdapter class
- `pubcast_codebase/main.py` — guarded imports for modules.wired.* (lines 294-316)
- `HANDOFF_2026-06-10.md` — this file

### 6. Full Downloads Sweep — Additional Files Placed (Jun 10)

**Core modules** → `modules/` (19 files):
`audio_devices.py` (fixes broken doctor.py import), `capture.py`, `cc_memory_store.py`,
`contradiction_engine.py`, `engine.py` (event bus/DAG), `evidence_layer.py`,
`integrity_checker.py`, `key_recovery.py`, `linux_enforcer.py`, `mic_routes.py`,
`pete_avatar.py`, `pete_repeat_character_profiles_v2.py`, `pubcast_vision_integration.py`,
`pubcast_vision_routes.py`, `purfluous.py`, `response_execution_layer.py`,
`secure_secrets.py`, `theory_graph.py`, `universal_avatar_builder_v1.py`

**Wired pipeline** → `modules/wired/` (7 files):
`director.py` (fixes broken response_composer.py import), `pubcast_runtime.py`,
`companion_core.py`, `acceptance_engine.py`, `conversation_pacing.py`,
`companion_runner.py`, `version_comparison.html`

**Rust renderer source** → `rust_crate/`:
`pubcast_renderer_built_on_claude.rs`, `pubcast_renderer_hardened.rs`

**Avatar asset** → `assets/`:
`pete_avatar_pubcast_v56.glb` (577KB)

**Frontend JS** → `static/` (8 files):
`animation_presets_complete.js`, `avatar_glb_motion_consumer.js`,
`ethereal_shader_system.js`, `ethereal_skin_shader_system_CORRECTED.js`,
`memory-manager.js`, `mic_device_picker.js`, `mic_processor.js`, `mic_profiles.js`

**Tests** → `tests/` (12 files + conftest.py):
`conftest.py`, `test_00_integrity.py`–`test_06_regression.py`,
`test_ai_characters.py`, `test_eq_transitions.py`, `test_memory_systems.py`,
`test_orchestrator_streaming.py`

**Tools** → `tools/`:
`build_pete_glb.py`, `pubcast_agent.py`

**Package init files created:**
`modules/wired/__init__.py`, `tests/__init__.py`, `tools/__init__.py`

### 7. Companion System — Missing Dependencies
The companion pipeline (`modules/wired/companion_core.py`, `acceptance_engine.py`,
`conversation_pacing.py`, `companion_runner.py`) imports 4 modules not yet in the codebase:
- `anti_parroting` — AntiParrotingMonitor, UserTurn, ResponseDraft, etc.
- `safeguards` — SafeguardLayer, SafeguardReport, EmotionalState, etc.
- `known_vs_novel` — KnownVsNovel, InsightCandidate, etc.
- `contradiction_tracker` — ContradictionTracker (this is NOT the same as `contradiction_engine.py`)

`meaning_maker` dependency IS satisfied (exists in `modules/wired/meaning_maker.py`).

## Files Changed/Created This Session (Updated)
- `pubcast_codebase/thinking_context/` — 8 files (Variant B, placed)
- `pubcast_codebase/modules/wired/` — 23 friday-Lunch + 6 companion/pipe + pubcast_adapter = 30 files
- `pubcast_codebase/modules/` — 19 new core modules
- `pubcast_codebase/modules/wired/pubcast_adapter.py` — PubCastAdapter class
- `pubcast_codebase/main.py` — guarded imports for modules.wired.* (lines 294-316)
- `pubcast_codebase/rust_crate/` — 2 Rust renderer files
- `pubcast_codebase/assets/pete_avatar_pubcast_v56.glb` — Pete avatar
- `pubcast_codebase/static/` — 8 JS files
- `pubcast_codebase/tests/` — 13 files (+ conftest, + init)
- `pubcast_codebase/tools/` — 2 files (+ init)
- `pubcast_codebase/modules/wired/__init__.py`, `tests/__init__.py`, `tools/__init__.py`
- `HANDOFF_2026-06-10.md` — this file

### 8. Playground Sweep — Additional Files Placed (Jun 10)

**Core modules** → `modules/` (5 files):
`ai_providers.py` (protocol-based AI provider abstraction, 10KB), `ai_runtime.py` (AI profile/config runtime, 8KB), `vision.py` (full camera vision system, 21KB), `documents.py` (document/message store, 9KB), `littlemode_protocol.py` (caregiving protocol, 21KB)

**Tests** → `tests/` (4 files):
`test_ai_runtime.py`, `test_pubcast_smoke.py`, `test_frontend_contract_compat.py`, `test_frontend_startup_contracts.py`

**Tools/PowerShell** → `tools/` (9 files):
`audit_glb_rigs.mjs` (GLB skeleton inspector), `PubCast_Brain_Diagnostic.py`, `pubcast_agent.py`,
`Build-PubCastCleanupReport.ps1`, `Finalize-ZipDuplicateGroups.ps1`, `Inventory-ZipsAndDuplicates.ps1`,
`Prove-ZipDuplicates.ps1`, `Stream-ProveZipDuplicates.ps1`, `Stream-ZipInventory.ps1`

**Config** → `config/`:
`ai_runtime.json` (AI runtime profiles for gemma3, gemma4, local GGUF, echo)

**Launcher** → `launcher/` (5 files):
`Pubcast program launcher.cs`, `PubCastLauncher.cs`, `PubCast_Launcher.sed`, `start_pubcast.cmd`, `start_pubcast.ps1`

**Frontend** → `static/`:
`pubcast_teleprompter.html` (teleprompter UI, 40KB)

## Files Changed/Created This Session (Final)
- `pubcast_codebase/thinking_context/` — 8 files (Variant B)
- `pubcast_codebase/modules/wired/` — 30 files (23 friday-Lunch + 6 companion/pipe + pubcast_adapter)
- `pubcast_codebase/modules/` — 24 new core modules total (19 Downloads + 5 Playground)
- `pubcast_codebase/modules/wired/pubcast_adapter.py`
- `pubcast_codebase/main.py` — guarded imports for modules.wired.*
- `pubcast_codebase/rust_crate/` — 2 Rust renderer files
- `pubcast_codebase/assets/` — 3 GLB files (manny, sheila, pete)
- `pubcast_codebase/static/` — 8 JS files + 1 HTML (teleprompter)
- `pubcast_codebase/tests/` — 17 test files + conftest + init
- `pubcast_codebase/tools/` — 11 files (Python + JS + PowerShell) + init
- `pubcast_codebase/config/ai_runtime.json`
- `pubcast_codebase/launcher/` — 5 Windows launcher files
- `pubcast_codebase/modules/wired/__init__.py`, `tests/__init__.py`, `tools/__init__.py`
- `HANDOFF_2026-06-10.md` — this file
